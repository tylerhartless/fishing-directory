name: Toggle Production Maintenance Mode

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Maintenance mode action'
        required: true
        type: choice
        options:
          - enable
          - disable
        default: 'enable'

jobs:
  toggle-maintenance:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Enable Maintenance Mode
        if: ${{ github.event.inputs.action == 'enable' }}
        run: |
          # Install lftp for FTP operations
          sudo apt-get update
          sudo apt-get install -y lftp

          # Create empty MAINTENANCE file
          touch MAINTENANCE

          # Upload MAINTENANCE file via FTP (single file upload, no sync)
          lftp -c "
          set ssl:verify-certificate no;
          open ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}
          cd /public_html
          put MAINTENANCE
          bye
          "
          echo "✅ Maintenance mode file uploaded"

      - name: Disable Maintenance Mode (Delete MAINTENANCE file)
        if: ${{ github.event.inputs.action == 'disable' }}
        run: |
          # Install lftp for FTP operations
          sudo apt-get update
          sudo apt-get install -y lftp

          # Delete MAINTENANCE file via FTP
          lftp -c "
          set ssl:verify-certificate no;
          open ftp://${{ secrets.FTP_USERNAME }}:${{ secrets.FTP_PASSWORD }}@${{ secrets.FTP_SERVER }}
          cd /public_html
          rm -f MAINTENANCE
          bye
          "
          echo "Maintenance mode disabled - MAINTENANCE file deleted"

      - name: Verify Action
        run: |
          if [ "${{ github.event.inputs.action }}" == "enable" ]; then
            echo "✅ Maintenance mode ENABLED on wherecanifish.com"
            echo "🔧 Site is now in maintenance mode"
            echo "ℹ️  To disable, run this workflow again and select 'disable'"
          else
            echo "✅ Maintenance mode DISABLED on wherecanifish.com"
            echo "🚀 Site is now live"
            echo "ℹ️  To enable, run this workflow again and select 'enable'"
          fi
